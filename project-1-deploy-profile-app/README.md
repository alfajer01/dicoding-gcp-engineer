# Aplikasi Profile

## Overview

A simple profile website built with HTML and CSS.

## Run / Deploy

Make sure the asset URLs in `index.html` are correct before deployment.

Deploy the app with:

```bash
gcloud app deploy
```
